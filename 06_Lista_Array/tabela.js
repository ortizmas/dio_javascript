// Imprima sua tabuada

console.log("\n");
const tabela = 12;
console.log(`Tabuada do ${tabela} \n`);
for (let index = 1; index <= 12; index++) {
	console.log(`${tabela} x ${index} = ${tabela * index}`);
}
